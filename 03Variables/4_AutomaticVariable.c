#include<stdio.h>
void main(){
    int x = 4; // local variable.
    auto int y = 5; // Automatic variable
}


// All variable in C that we are declared inside the block, are automatic variables by default.
// We can explicitly declare an automatic variable using auto keyword.

